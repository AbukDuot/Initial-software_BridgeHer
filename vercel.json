{
  "buildCommand": "cd bridgeher-frontend && npm install && npm run build",
  "outputDirectory": "bridgeher-frontend/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
